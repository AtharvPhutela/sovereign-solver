# Build trees
build/
build-*/
cmake-build-*/
compile_commands.json

# Out-of-tree benchmark oracle (tools/oracle/install_highs.sh builds HiGHS here).
# Kept entirely outside version control and outside the sovereignty-scanned tree
# -- see docs/dependency-ledger.md and sovereignty.toml exclude_globs.
build-oracle/

# Downloaded benchmark corpora (ticket #2). Only benchmarks/smoke/ is committed.
benchmarks/data/
benchmarks/.tools/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Oracle scratch
*.sol

# HiGHS CLI writes this in its CWD
HiGHS.log
build64/

# Claude Code local settings
.claude/
